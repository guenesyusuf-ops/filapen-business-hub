import { PlaceholderCard } from '../_placeholder';

export default function MonatPage() {
  return (
    <PlaceholderCard
      title="Monatsansicht"
      subtitle="Tages-Tabelle mit sticky Spalten + Tages-Drawer"
      phase="Phase 3–4"
      description="Hier entsteht das Kernstück: eine kompakte Tabelle mit einem Datensatz pro Kalendertag, sticky Datum links + sticky Ergebnis rechts. Column Groups je Kanal (Umsätze / Werbung / Kosten / Ergebnis), gespeicherte Ansichten (Kompakt / Marketing / Webshop / Amazon / TikTok / Profit), Inline-Edit, Autosave, Klick auf Datum öffnet Tages-Drawer mit allen Feldern."
    />
  );
}
