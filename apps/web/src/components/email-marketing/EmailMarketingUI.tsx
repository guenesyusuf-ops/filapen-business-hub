'use client';

// Re-exports only (no JSX here — SWC + Next.js gets confused with mixed re-export + JSX)
export { Badge, KpiCard, PageHeader, Money, Empty, btn, input, label } from '@/components/purchases/PurchaseUI';
export { SoonBadge, SectionCard } from './EmailExtras';
