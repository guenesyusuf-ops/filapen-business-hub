'use client';

// Re-exports only (same pattern as ShippingUI) — keeps the sales pages visually
// consistent with purchases/shipping without duplicating primitives.
export { Badge, KpiCard, PageHeader, Money, Empty, btn, input, label } from '@/components/purchases/PurchaseUI';
export { SoonBadge, SectionCard } from '@/components/email-marketing/EmailExtras';
